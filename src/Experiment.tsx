import Button from '@mui/material/Button'
import type { FunctionComponent } from 'react'

export const Experiment: FunctionComponent = () => {
	return (
		<div>
			Experiment <Button variant="contained">Hello world</Button>;
		</div>
	)
}
